export const MAP_PIXEL_WIDTH = 6016;
export const MAP_PIXEL_HEIGHT = 3140;

// Scene plane dimensions (in world units)
export const PLANE_WIDTH = 60;
export const PLANE_HEIGHT = (MAP_PIXEL_HEIGHT / MAP_PIXEL_WIDTH) * PLANE_WIDTH;
